- Remove CSS modules (W3C uses different approach, referencing classes from
  other modules is not possible). Currently there is a bug in Footer.
- CSS for Markdown content doesn't load when navigating internally (e.g. from
  home to talks).
- Page has white border.
- Use OIDC in GitHub action build step
