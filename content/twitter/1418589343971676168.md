---
favorite_count: 0
retweet_count: 0
created_at: "2021-07-23T15:10:30.000Z"
lang: en
full_text: >-
  @nilshartmann The problem is that I have a tree of components, not all
  components are rendered, and some of the conditionally rendered components
  have state. That's where the error comes from 


  Typical scenario:

  if(error) return renderError()

  return render()
---

[@nilshartmann](https://twitter.com/nilshartmann) The problem is that I have a
tree of components, not all components are rendered, and some of the
conditionally rendered components have state. That's where the error comes from

Typical scenario: if(error) return renderError() return render()
